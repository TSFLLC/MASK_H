USE [TERA];
GO
SET NOCOUNT ON;

UPDATE h
SET
    EMPLOYEE =
        CASE 
            WHEN h.EMPLOYEE IS NULL
            THEN NULL
            ELSE ABS(CAST(CHECKSUM(s.Seed + '_EMP') AS BIGINT)) % 90000000 + 10000000
        END,

    FIRST_NAME =
        CASE
            WHEN h.FIRST_NAME IS NULL OR NULLIF(LTRIM(RTRIM(h.FIRST_NAME)), '') IS NULL
                 OR UPPER(LTRIM(RTRIM(h.FIRST_NAME))) IN ('NONE','NULL','N/A','NA')
            THEN h.FIRST_NAME
            ELSE CASE ABS(CAST(CHECKSUM(s.Seed + '_FN') AS BIGINT)) % 10
                WHEN 0 THEN 'James' WHEN 1 THEN 'Mary' WHEN 2 THEN 'John' WHEN 3 THEN 'Patricia' WHEN 4 THEN 'Robert'
                WHEN 5 THEN 'Linda' WHEN 6 THEN 'Michael' WHEN 7 THEN 'Barbara' WHEN 8 THEN 'David' WHEN 9 THEN 'Elizabeth'
            END
        END,

    MIDDLE_INIT =
        CASE
            WHEN h.MIDDLE_INIT IS NULL OR NULLIF(LTRIM(RTRIM(h.MIDDLE_INIT)), '') IS NULL
                 OR UPPER(LTRIM(RTRIM(h.MIDDLE_INIT))) IN ('NONE','NULL','N/A','NA')
            THEN h.MIDDLE_INIT
            ELSE CHAR(65 + (ABS(CAST(CHECKSUM(s.Seed + '_MI') AS BIGINT)) % 26))
        END,

    MIDDLE_NAME =
        CASE
            WHEN h.MIDDLE_NAME IS NULL OR NULLIF(LTRIM(RTRIM(h.MIDDLE_NAME)), '') IS NULL
                 OR UPPER(LTRIM(RTRIM(h.MIDDLE_NAME))) IN ('NONE','NULL','N/A','NA')
            THEN h.MIDDLE_NAME
            ELSE CASE ABS(CAST(CHECKSUM(s.Seed + '_MN') AS BIGINT)) % 10
                WHEN 0 THEN 'Allen' WHEN 1 THEN 'Marie' WHEN 2 THEN 'Lee' WHEN 3 THEN 'Grace' WHEN 4 THEN 'Ray'
                WHEN 5 THEN 'Ann' WHEN 6 THEN 'Scott' WHEN 7 THEN 'Jean' WHEN 8 THEN 'Paul' WHEN 9 THEN 'Lynn'
            END
        END,

    LAST_NAME =
        CASE
            WHEN h.LAST_NAME IS NULL OR NULLIF(LTRIM(RTRIM(h.LAST_NAME)), '') IS NULL
                 OR UPPER(LTRIM(RTRIM(h.LAST_NAME))) IN ('NONE','NULL','N/A','NA')
            THEN h.LAST_NAME
            ELSE CASE ABS(CAST(CHECKSUM(s.Seed + '_LN') AS BIGINT)) % 10
                WHEN 0 THEN 'Smith' WHEN 1 THEN 'Johnson' WHEN 2 THEN 'Williams' WHEN 3 THEN 'Brown' WHEN 4 THEN 'Jones'
                WHEN 5 THEN 'Garcia' WHEN 6 THEN 'Miller' WHEN 7 THEN 'Davis' WHEN 8 THEN 'Rodriguez' WHEN 9 THEN 'Martinez'
            END
        END,

    SUPERVISORS_EMP_NUM =
        CASE
            WHEN h.SUPERVISORS_EMP_NUM IS NULL
            THEN NULL
            ELSE ABS(CAST(CHECKSUM(s.Seed + '_SUPNUM') AS BIGINT)) % 90000000 + 10000000
        END,

    SUPERVISOR =
        CASE
            WHEN h.SUPERVISOR IS NULL OR NULLIF(LTRIM(RTRIM(h.SUPERVISOR)), '') IS NULL
                 OR UPPER(LTRIM(RTRIM(h.SUPERVISOR))) IN ('NONE','NULL','N/A','NA')
            THEN h.SUPERVISOR
            ELSE CASE ABS(CAST(CHECKSUM(s.Seed + '_SUP') AS BIGINT)) % 10
                WHEN 0 THEN 'Anderson' WHEN 1 THEN 'Thomas' WHEN 2 THEN 'Jackson' WHEN 3 THEN 'White' WHEN 4 THEN 'Harris'
                WHEN 5 THEN 'Martin' WHEN 6 THEN 'Thompson' WHEN 7 THEN 'Moore' WHEN 8 THEN 'Taylor' WHEN 9 THEN 'Clark'
            END
        END
-- keep the rest of your script unchanged
FROM [S_people].[HR_Snapshots] h
CROSS APPLY (
    SELECT Seed =
        COALESCE(
            NULLIF(LTRIM(RTRIM(
                COALESCE(CONVERT(varchar(200), h.EMPLOYEE), '') + '|' +
                COALESCE(h.FIRST_NAME, '') + '|' +
                COALESCE(h.LAST_NAME, '') + '|' +
                COALESCE(h.EMAIL_ADDRESS, '') + '|' +
                COALESCE(CONVERT(varchar(10), h.BIRTHDATE, 120), '')
            )), ''),
            CONVERT(varchar(36), NEWID())
        )
) s
CROSS APPLY (
    SELECT MaskedBirthdate =
        CASE
            WHEN h.BIRTHDATE IS NULL THEN NULL
            ELSE DATEADD(DAY, ABS(CAST(CHECKSUM(s.Seed + '_DOB') AS BIGINT)) % 25550, CONVERT(date,'1940-01-01'))
        END
) bd;
GO


+++++++++++++++++++++++++++++++/////+trigger +++++++++++++++++++++++++++++++++++++++++++

USE [TERA];
GO
SET NOCOUNT ON;

UPDATE h
SET
    EMPLOYEE =
        CASE 
            WHEN h.EMPLOYEE IS NULL
            THEN NULL
            ELSE ABS(CAST(CHECKSUM(s.Seed + '_EMP') AS BIGINT)) % 90000000 + 10000000
        END,

    FIRST_NAME =
        CASE
            WHEN h.FIRST_NAME IS NULL OR NULLIF(LTRIM(RTRIM(h.FIRST_NAME)), '') IS NULL
                 OR UPPER(LTRIM(RTRIM(h.FIRST_NAME))) IN ('NONE','NULL','N/A','NA')
            THEN h.FIRST_NAME
            ELSE CASE ABS(CAST(CHECKSUM(s.Seed + '_FN') AS BIGINT)) % 10
                WHEN 0 THEN 'James' WHEN 1 THEN 'Mary' WHEN 2 THEN 'John' WHEN 3 THEN 'Patricia' WHEN 4 THEN 'Robert'
                WHEN 5 THEN 'Linda' WHEN 6 THEN 'Michael' WHEN 7 THEN 'Barbara' WHEN 8 THEN 'David' WHEN 9 THEN 'Elizabeth'
            END
        END,

    MIDDLE_INIT =
        CASE
            WHEN h.MIDDLE_INIT IS NULL OR NULLIF(LTRIM(RTRIM(h.MIDDLE_INIT)), '') IS NULL
                 OR UPPER(LTRIM(RTRIM(h.MIDDLE_INIT))) IN ('NONE','NULL','N/A','NA')
            THEN h.MIDDLE_INIT
            ELSE CHAR(65 + (ABS(CAST(CHECKSUM(s.Seed + '_MI') AS BIGINT)) % 26))
        END,

    MIDDLE_NAME =
        CASE
            WHEN h.MIDDLE_NAME IS NULL OR NULLIF(LTRIM(RTRIM(h.MIDDLE_NAME)), '') IS NULL
                 OR UPPER(LTRIM(RTRIM(h.MIDDLE_NAME))) IN ('NONE','NULL','N/A','NA')
            THEN h.MIDDLE_NAME
            ELSE CASE ABS(CAST(CHECKSUM(s.Seed + '_MN') AS BIGINT)) % 10
                WHEN 0 THEN 'Allen' WHEN 1 THEN 'Marie' WHEN 2 THEN 'Lee' WHEN 3 THEN 'Grace' WHEN 4 THEN 'Ray'
                WHEN 5 THEN 'Ann' WHEN 6 THEN 'Scott' WHEN 7 THEN 'Jean' WHEN 8 THEN 'Paul' WHEN 9 THEN 'Lynn'
            END
        END,

    LAST_NAME =
        CASE
            WHEN h.LAST_NAME IS NULL OR NULLIF(LTRIM(RTRIM(h.LAST_NAME)), '') IS NULL
                 OR UPPER(LTRIM(RTRIM(h.LAST_NAME))) IN ('NONE','NULL','N/A','NA')
            THEN h.LAST_NAME
            ELSE CASE ABS(CAST(CHECKSUM(s.Seed + '_LN') AS BIGINT)) % 10
                WHEN 0 THEN 'Smith' WHEN 1 THEN 'Johnson' WHEN 2 THEN 'Williams' WHEN 3 THEN 'Brown' WHEN 4 THEN 'Jones'
                WHEN 5 THEN 'Garcia' WHEN 6 THEN 'Miller' WHEN 7 THEN 'Davis' WHEN 8 THEN 'Rodriguez' WHEN 9 THEN 'Martinez'
            END
        END,

    SUPERVISORS_EMP_NUM =
        CASE
            WHEN h.SUPERVISORS_EMP_NUM IS NULL
            THEN NULL
            ELSE ABS(CAST(CHECKSUM(s.Seed + '_SUPNUM') AS BIGINT)) % 90000000 + 10000000
        END,

    SUPERVISOR =
        CASE
            WHEN h.SUPERVISOR IS NULL OR NULLIF(LTRIM(RTRIM(h.SUPERVISOR)), '') IS NULL
                 OR UPPER(LTRIM(RTRIM(h.SUPERVISOR))) IN ('NONE','NULL','N/A','NA')
            THEN h.SUPERVISOR
            ELSE CASE ABS(CAST(CHECKSUM(s.Seed + '_SUP') AS BIGINT)) % 10
                WHEN 0 THEN 'Anderson' WHEN 1 THEN 'Thomas' WHEN 2 THEN 'Jackson' WHEN 3 THEN 'White' WHEN 4 THEN 'Harris'
                WHEN 5 THEN 'Martin' WHEN 6 THEN 'Thompson' WHEN 7 THEN 'Moore' WHEN 8 THEN 'Taylor' WHEN 9 THEN 'Clark'
            END
        END
-- keep the rest of your script unchanged
FROM [S_people].[HR_Snapshots] h
CROSS APPLY (
    SELECT Seed =
        COALESCE(
            NULLIF(LTRIM(RTRIM(
                COALESCE(CONVERT(varchar(200), h.EMPLOYEE), '') + '|' +
                COALESCE(h.FIRST_NAME, '') + '|' +
                COALESCE(h.LAST_NAME, '') + '|' +
                COALESCE(h.EMAIL_ADDRESS, '') + '|' +
                COALESCE(CONVERT(varchar(10), h.BIRTHDATE, 120), '')
            )), ''),
            CONVERT(varchar(36), NEWID())
        )
) s
CROSS APPLY (
    SELECT MaskedBirthdate =
        CASE
            WHEN h.BIRTHDATE IS NULL THEN NULL
            ELSE DATEADD(DAY, ABS(CAST(CHECKSUM(s.Seed + '_DOB') AS BIGINT)) % 25550, CONVERT(date,'1940-01-01'))
        END
) bd;
GO


++++++++++++++++++++++++////dynamic masking++++++++++++++++++++++++++++++++++++
USE [TERA];
GO

-- Remove existing masks first if needed before re-adding.
-- Apply Dynamic Data Masking

ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [EMPLOYEE]            varchar(200) MASKED WITH (FUNCTION = 'default()');
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [FIRST_NAME]          varchar(200) MASKED WITH (FUNCTION = 'partial(1,"XXXX",0)');
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [MIDDLE_INIT]         varchar(50)  MASKED WITH (FUNCTION = 'partial(0,"X",0)');
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [MIDDLE_NAME]         varchar(200) MASKED WITH (FUNCTION = 'partial(1,"XXXX",0)');
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [LAST_NAME]           varchar(200) MASKED WITH (FUNCTION = 'partial(1,"XXXX",0)');
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [SUPERVISORS_EMP_NUM] varchar(200) MASKED WITH (FUNCTION = 'default()');
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [SUPERVISOR]          varchar(200) MASKED WITH (FUNCTION = 'partial(1,"XXXX",0)');
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [BIRTHDATE]           date         MASKED WITH (FUNCTION = 'default()');
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [AGE]                 int          MASKED WITH (FUNCTION = 'default()');
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [SEX]                 varchar(20)  MASKED WITH (FUNCTION = 'partial(0,"X",0)');
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [DEATH_DATE]          date         MASKED WITH (FUNCTION = 'default()');
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [EMAIL_ADDRESS]       varchar(254) MASKED WITH (FUNCTION = 'email()');
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [ANNIVERS_DATE]       date         MASKED WITH (FUNCTION = 'default()');
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [NEW_HIRE_DATE]       date         MASKED WITH (FUNCTION = 'default()');
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [DATE_HIRED]          date         MASKED WITH (FUNCTION = 'default()');
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [ADJ_HIRE_DATE]       date         MASKED WITH (FUNCTION = 'default()');
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [FST_DAY_WORKED]      date         MASKED WITH (FUNCTION = 'default()');
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [TERM_DATE]           date         MASKED WITH (FUNCTION = 'default()');
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [LAST_DAY_PAID]       date         MASKED WITH (FUNCTION = 'default()');
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [Created_By]          varchar(200) MASKED WITH (FUNCTION = 'partial(1,"XXXX",0)');
GO

+++++++++++++++++++++++++++++///remove masking+++++++++++

USE [TERA];
GO

ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [EMPLOYEE] DROP MASKED;
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [FIRST_NAME] DROP MASKED;
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [MIDDLE_INIT] DROP MASKED;
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [MIDDLE_NAME] DROP MASKED;
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [LAST_NAME] DROP MASKED;
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [SUPERVISORS_EMP_NUM] DROP MASKED;
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [SUPERVISOR] DROP MASKED;

ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [BIRTHDATE] DROP MASKED;
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [AGE] DROP MASKED;
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [SEX] DROP MASKED;
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [DEATH_DATE] DROP MASKED;

ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [EMAIL_ADDRESS] DROP MASKED;

ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [ANNIVERS_DATE] DROP MASKED;
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [NEW_HIRE_DATE] DROP MASKED;
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [DATE_HIRED] DROP MASKED;
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [ADJ_HIRE_DATE] DROP MASKED;
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [FST_DAY_WORKED] DROP MASKED;
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [TERM_DATE] DROP MASKED;
ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [LAST_DAY_PAID] DROP MASKED;

ALTER TABLE [S_people].[HR_Snapshots] ALTER COLUMN [Created_By] DROP MASKED;

GO

+++++++++++++++++++++verify++++++++++++++++++++++++++++++++++++++++++++
SELECT
t.name AS TableName,
c.name AS ColumnName,
mc.masking_function
FROM sys.masked_columns mc
JOIN sys.columns c
    ON mc.object_id = c.object_id
    AND mc.column_id = c.column_id
JOIN sys.tables t
    ON mc.object_id = t.object_id
WHERE t.name = 'HR_Snapshots';

